# Copy to .env, then enter your own key with a private editor.
# The file is parsed as data; do not source it in a shell.
RUNPOD_API_KEY=
# ComfyUI uses the private SSH tunnel; no second key is normally needed.
